# Screenshots to add

The GSoC report (`_posts/2026-09-22-gsoc-2026-final-report.md`) already has `<figure>` blocks wired up for these —
drop a PNG/JPG in with the exact filename below and it will appear in the post automatically, no other edit needed.

| Filename | What to capture |
|---|---|
| `screenshot-library.png` | The Streamlit **paper library** page — the list of papers with their processed / extracted / error status. |
| `screenshot-workspace.png` | The **two-pane review workspace**: the source PDF on the left, the extracted record list on the right. |
| `screenshot-field-review.png` | One expanded record showing a few `result \| source \| actions` field rows (the quoted evidence + approve/edit icons). |
| `screenshot-pdf-evidence.png` | The PDF pane after clicking a field's eye icon — the source block highlighted on the actual page. |

Keep them reasonably sized (~1400px wide is plenty) so the page loads fast. Any format `<img>` supports works; if you
use a different extension (`.jpg`, `.webp`, …), update the matching `src=` in the post file to match.
